# API base URL for backend (defaults to /api for same-origin requests)
VITE_API_BASE_URL=/api

# Optional: Full backend URL for development (Vite will proxy /api/* to this)
# VITE_BACKEND_URL=http://localhost:8080
